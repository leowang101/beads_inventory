# ==== MySQL ====
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=
DB_PASS=
DB_NAME=

# ==== Server ====
PORT=3000
# 是否由后端托管前端（true/false）
SERVE_FRONTEND=true

# ==== Qwen3-VL-Plus (阿里云百炼 / DashScope OpenAI兼容接口) ====
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_VL_MODEL=qwen3-vl-flash

# === OSS / Upload (STS via ECS RAM Role) ===
OSS_REGION=
OSS_BUCKET=

# 直传上传域名（CNAME）
OSS_UPLOAD_ENDPOINT=
OSS_UPLOAD_CNAME=true

# 展示/下载统一走 CDN 域名
OSS_CDN_BASE_URL=

# ECS 绑定的 RAM 角色名（用于从元数据获取 STS）
ECS_RAM_ROLE_NAME=

# ECS 元数据服务地址（一般不用改，写上便于显式配置/测试）
ECS_METADATA_BASE_URL=http://100.100.100.200/latest/meta-data/ram/security-credentials

# （可选）推荐上传路径前缀（你也可以在代码里拼 patterns/<uid>/）
OSS_UPLOAD_PREFIX=patterns
